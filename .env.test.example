# Test Environment Configuration
# Copy to .env.test and customize for your test environment

NODE_ENV=test

# Database - Use a SEPARATE test database (will be modified during tests!)
# IMPORTANT: Never point this at production or development data
DATABASE_URL="postgresql://postgres:postgres@localhost:5432/paper_trading_test?schema=public"

# Redis - Can share with dev or use separate instance
REDIS_URL="redis://localhost:6379"

# Optional: Disable logging during tests
# LOG_LEVEL=silent
